programa pelos;
vars
    i, j : entero;

entero uno(x : entero) {
    vars
        r : entero;
    {
        r = x * 2;
    }
};

entero dos(x : entero) {
    vars
        r : entero;
    {
        r = x * uno(x);
    }
};

inicio
{
    i = 5;
    escribe("dos(i+3-1) =", dos(i + 3 - 1));
}
fin
